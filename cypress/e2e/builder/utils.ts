export const buildItemPath = (id: string) => `/builder/items/${id}`;
export const buildItemSharePath = (id: string) => `${buildItemPath(id)}/share`;
export const HOME_PATH = '/builder';
export const BOOKMARKED_ITEMS_PATH = '/builder/bookmarks';
